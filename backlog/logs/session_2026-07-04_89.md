# Session 2026-07-04 — N°89

**Sprint actif :** 22 — État TV : contrat back
**Tickets traités :** 2

## Spec Review

| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/technical/tv-state.md` | ❌ Dérive bloquante (attendu) | 0 — même constat qu'en session #88 : le contrat cible (`tv/state`, `tv/idle`) n'était pas encore codé au moment de la review (début de session), l'écart est intégralement couvert par les issues déjà ouvertes du sprint |

## Tickets clôturés

| # | Titre | Verdict |
|---|-------|---------|
| #254 | `GET /api/tv/idle/` — contenu du carousel | ✅ Approuvé |

## Tickets à reprendre

| # | Titre | Raison |
|---|-------|--------|
| #253 | `GET /api/tv/state/` — hero + stake + next unifié | ❌ À corriger — `_pack_tv_stake` (`live/api_views.py`) utilise `hero.event.name`, champ inexistant sur le modèle `Event` (seul `event.category.name` existe). `AttributeError` → 500 sur le chemin nominal (hero LIVE avec stake résolvable). Piste : remplacer par `hero.event.category.name` aux deux occurrences (~lignes 1885, 1892). Reste de la livraison conforme (contrat spec, réutilisation `get_tv_next`/`_pack_match`/`_pack_event_bracket`, endpoint public, legacy intact). Label `à-reprendre` posé, détail complet dans `backlog/plan/253-tv-state-endpoint.md` (section problème bloquant) et le commentaire GitHub de l'issue.

## Nouveaux tickets créés

Aucun.

## Fichiers partagés câblés

- `live/urls.py` : ajout de `path("api/tv/state/", api_views.api_tv_state, name="api_tv_state")` (#253) et `path("api/tv/idle/", api_views.api_tv_idle, name="api_tv_idle")` (#254), à côté de `api/tv/upcoming/` existant.

## Problèmes d'orchestration

Aucun écart au protocole. Le ticket #253 a suivi le chemin `❌ À corriger` du protocole normalement (label `à-reprendre`, commentaire GitHub, section « Problème bloquant » ajoutée au plan, commit avec l'emoji 🚧) ; le ticket #256 (CRUD Announcement, mineure) n'a pas été traité — budget de 2 tickets par session atteint avec #253 et #254.

## Git

- Branche : `claude/sprint/22-tv-state-back`
- Parent effectif : `claude/sprint/21-durcissements-api-specs` (résolu via `backlog/sprints/done/` — toujours pas mergée dans `origin/main` ; merge sans conflit : already up to date)
- Push : ✅ succès (voir plus bas)

| Hash court | Message du commit |
|-----------|------------------|
| `fb0cfa3` | 22-253 🚧 Back : GET /api/tv/state/ (bug hero.event.name à corriger) |
| `03643cd` | 22-254 ✅ Back : GET /api/tv/idle/ — contenu du carousel |
