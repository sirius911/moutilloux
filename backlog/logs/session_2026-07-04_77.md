# Session 2026-07-04 — N°77

**Sprint actif :** 21 — Durcissements API & specs
**Tickets traités :** 2

## Spec Review
| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/screens/admin-tournoi.md` | ⚠️ Dérive mineure (résolue dans cette session, #211) | 0 |
| `specs/technical/planning.md` | ⚠️ Dérive mineure (couverte par #213/#214/#215/#216, déjà ticketées) | 0 |
| `specs/technical/cycle-de-vie-epreuve.md` | ⚠️ Dérive mineure (marqueurs « à créer » périmés, #217/#218, déjà ticketées) | 0 |
| `specs/technical/cycle-de-vie-match.md` | ⚠️ Dérive mineure (table champs à jour sauf `end_reason`, `@referee_required` déjà posé mais listé « neuf » — relève de #218) | 0 |

Toutes les dérives constatées en code correspondent exactement aux tickets déjà
ouverts du sprint (#211-219, origine : audit externe du 2026-07-02). Aucune
nouvelle issue créée. Détail vérifié en code : `Event.status`/`has_third_place`
déjà présents (`competition/models.py`), `PlayDay`/`Break` déjà créés
(`live/models.py`), `Entry.withdrawn`/`Match.is_walkover` déjà présents,
`Match.end_reason` toujours absent (marqueur correct), `@referee_required` déjà
posé sur `api_arbitre_matches` (marqueur « neuf » périmé, relève de #218),
`_resolve_court_pk` fait toujours du `get_or_create` (#213), `scheduled_time`/
`court` toujours éditables via `api_match_edit` (#214), `delete_play_day`
refuse toujours si matchs terminés sans qu'une décision produit soit tranchée
(#215), `default_match_duration_min` toujours sans UI dans `EditionModal.vue`
(#216), `create_edition` avait toujours le paramètre `activate` côté serveur
avant résolution de #211 dans cette session.

## Tickets clôturés
| # | Titre | Verdict |
|---|-------|---------|
| #211 | Activation automatique de la première édition implémentée côté front seulement | ✅ Approuvé |
| #212 | Auth du registre incohérente (api_players en login_required) + matrice d'accès non documentée | ✅ Approuvé |

## Nouveaux tickets créés
| # | Titre | Sévérité |
|---|-------|----------|
| — | aucun | — |

## Tickets à reprendre
| # | Titre | Raison |
|---|-------|--------|
| — | aucun | — |

## Fichiers partagés câblés
- `frontend/app/src/stores/event.ts` : retrait du champ mort `activate?: boolean` du type `EditionPayload` (#211), une fois l'agent d'implémentation confirmé qu'aucun autre call site ne le consommait.
- `live/urls.py` : non touché (aucune route ajoutée/retirée ce tour).

## Problèmes d'orchestration
Aucun. Note : un appel accidentel au tool Agent avec un prompt "placeholder" a
été lancé par erreur en tout début de session (avant le traitement de #211) ;
il n'a touché aucun fichier et s'est terminé immédiatement sans effet — pas de
nettoyage nécessaire.

## Git
- Branche : `claude/sprint/21-durcissements-api-specs`
- Parent effectif : `claude/sprint/20-transverse-erreurs-routing` (sprint 20
  toujours non mergé dans `main` — point récurrent, cf. sessions précédentes,
  à traiter côté humain)
- Push : voir résultat ci-dessous

| Hash court | Message du commit |
|-----------|------------------|
| `273563f` | 21-211 ✅ Activation auto 1ère édition — déplacée côté serveur |
| `46f6bee` | 21-212 ✅ Auth registre durcie + matrice d'accès /api/ documentée |
