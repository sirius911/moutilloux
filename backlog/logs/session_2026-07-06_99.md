# Session 2026-07-06 — N°99

**Sprint actif :** 24 — Affiches de match
**Tickets traités :** 2

## Spec Review

| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/technical/affiche-match.md` | ✅ Conforme | 0 |
| `specs/screens/admin-joueurs.md` | ✅ Conforme (partie livrée) | 0 |
| `specs/screens/admin-matchs.md` | ✅ Conforme (partie livrée) | 0 |
| `specs/screens/tv-live.md` | ✅ Conforme (partie livrée) | 0 |

0 dérive sur les parties déjà livrées (#263-268). Le reste attend correctement
#269-272, sans câblage spéculatif ni régression.

## Tickets clôturés

| # | Titre | Verdict |
|---|-------|---------|
| #269 | Fiche joueur — photo avec aperçu + attitude ; avatar registre | ✅ Approuvé |

## Tickets à reprendre

| # | Titre | Raison |
|---|-------|--------|
| #270 | Onglet « Affiche » d'EditMatchPanel + raccourci ligne calendrier | ❌ Bloquant : `selectCandidate()`/`removePoster()` émettent `saved`, qui ferme le panneau (`onMatchSaved` → `editingMatch.value = null`) alors que la spec ne demande aucune fermeture automatique après Choisir/Retirer — casse le golden path. Commentaire détaillé + piste postés sur l'issue, bloc `## ❌ Problème bloquant` ajouté au plan `backlog/plan/270-onglet-affiche-editmatchpanel.md`. |

## Nouveaux tickets créés

Aucun.

## Fichiers partagés câblés

- `live/api_views.py` (`_pack_entry`) : ajout de `attitude`/`photoUrl` sur `Entry.player` — gap détecté à la planification de #270 (pré-remplissage du formulaire attitude impossible sans ça). Commit dédié `5f8bc78`.
- `frontend/app/src/types/index.ts` : nouvelle interface `EntryPlayer` (miroir de l'ajout back ci-dessus), utilisée par `Entry.player` — commit `5f8bc78`. Puis extension de l'interface `Player` (`photoUrl`, `attitude`) pour #269 — commit `7f86705`.
- `live/admin_views.py` (`PlayerForm.Meta.fields`) : ajout de `"attitude"` + label — gap détecté à la planification de #269 (le formulaire d'édition ignorait silencieusement `attitude`). Commit `7f86705`.
- `live/api_views.py` (`_pack_player`, `api_player_create`) : exposition/persistance de `attitude` — même gap, même commit.
- `frontend/app/src/stores/event.ts` (`PlayerEditPayload`) : ajout de `attitude?: string` — commit `7f86705`.

Dans les deux cas (#270 et #269), le gap a été détecté par l'agent de
planification (lecture du code, pas supposition), et corrigé directement par
l'orchestrateur avant de lancer l'agent d'implémentation front, conformément
à la réservation CLAUDE.md §3/§5 de ces fichiers de contention.

## Problèmes d'orchestration

Aucun écart de protocole. Type-check (`npx vue-tsc -b --force`) revérifié
après chaque câblage orchestrateur et après chaque implémentation : toujours
seulement les 10 erreurs pré-existantes (#278), aucune régression introduite.

## Git

- Branche : `claude/sprint/24-affiches-match`
- Parent effectif : `claude/sprint/23-tv-live-front` (résolu via `backlog/sprints/done/` — sprint 23 toujours pas mergé dans `origin/main`)
- Push : ✅ succès

| Hash court | Message du commit |
|-----------|------------------|
| `76a166a` | 24 spec-review ✅ session #99 — 0 dérive, 4 issues sprint restantes |
| `5f8bc78` | 24-270 🚧 orchestrateur : attitude+photoUrl sur Entry.player (_pack_entry) |
| `48810ce` | 24-270 🚧 Front : onglet Affiche EditMatchPanel + raccourci ligne |
| `7f86705` | 24-269 🚧 orchestrateur : attitude sur Player (form+_pack_player+create) + types front |
| `57ee1a4` | 24-269 ✅ Front : fiche joueur photo+attitude, avatar registre |
