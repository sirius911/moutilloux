# Session 2026-07-03 — N°58

**Sprint actif :** 18 — Tableau final conforme (1ʳᵉ session).
**Tickets traités :** 2 — #196, #197.

## Spec Review
| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/screens/admin-tableau-final.md` | ❌ Dérive bloquante | 1 nouvelle (#230, mineure) + 7 dérives attendues (déjà ticketées #195-200, #176) |

Sprint venant de démarrer (aucun ticket traité avant cette session) : les 7
dérives connues sont normales à ce stade — elles correspondent exactement au
périmètre planifié du sprint (P3 exclue du bracket, recréation forcée
inopérante, sélecteur d'épreuve manquant, pas de polling, `confirm()` natif,
« Créer le tableau » proposé en INSCRIPTION, `stage_label` incomplet pour P3).
Une 8ᵉ dérive non ticketée a été trouvée : le type front `Bracket.p3` déclaré
optionnel alors que l'API le renvoie toujours (`live/api_views.py:595-600` vs
`frontend/app/src/types/index.ts:193`) — sans impact fonctionnel actuel,
ticketée en #230.

## Tickets clôturés
| # | Titre | Verdict |
|---|-------|---------|
| [#196](https://github.com/sirius911/moutilloux/issues/196) | Back : P3 exclue de bracket-labels / assign / clear → 404 sur la petite finale | ✅ Approuvé |
| [#197](https://github.com/sirius911/moutilloux/issues/197) | Back : api_bracket_create ignore start_stage/force — recréation forcée inopérante | ✅ Approuvé (avec réserve corrigée avant clôture) |

### Détail #196
`Match.Stage.P3` ajouté aux 5 querysets `stage__in` concernés :
`_final_matches_qs`, `assign_bracket_entry`, `clear_bracket_entry`,
`panel_final_label_update` (`live/admin_views.py`) et `api_bracket_labels`
(`live/api_views.py`). QF/SF/F conservés (aucune régression). `manage.py
check` OK. `live/urls.py` non touché.

### Détail #197
Nouvelle fonction de service `recreate_final_bracket_for_event(event,
force=False)` dans `live/bracket.py` : garde inconditionnelle (LIVE/FINISHED →
`ValueError`/400, avec ou sans `force`), suppression réelle des matchs
`SCHEDULED` du tableau puis régénération via `ensure_final_bracket_exists`
(canonique, inchangée) si `force=True`. `api_bracket_create`
(`live/api_views.py`) branché dessus, `start_stage` retiré du contrat.
Testé fonctionnellement en shell Django (rollback) : idempotence `force=False`,
suppression+régénération `force=True`, garde LIVE bloquante dans les deux cas.
Le reviewer a rendu ⚠️ Approuvé avec réserves (doc `roadmap/phase-7-bracket.md`
obsolète, ancien contrat `start_stage`) — corrigé par l'orchestrateur avant
clôture (fichier non versionné, `roadmap/` gitignoré). Le reviewer a aussi
signalé (hors périmètre du ticket, non modifié) que le front continue d'envoyer
`start_stage` dans le body — ticketé séparément en #231 plutôt que traité ici,
pour respecter le périmètre strict du ticket #197.

## Nouveaux tickets créés
| # | Titre | Sévérité |
|---|-------|----------|
| [#230](https://github.com/sirius911/moutilloux/issues/230) | Bracket.p3 typé optionnel côté front alors que l'API le renvoie toujours | 🟡 mineure |
| [#231](https://github.com/sirius911/moutilloux/issues/231) | AdminBracket : nettoyer start_stage, obsolète côté serveur depuis #197 | 🟡 mineure |

## Tickets à reprendre
| # | Titre | Raison |
|---|-------|--------|
| — | — | — |

## Fichiers partagés câblés
- Aucun. `live/urls.py` non touché sur les deux tickets (routes déjà existantes).

## Problèmes d'orchestration
Pour #197, le reviewer a rendu `⚠️ Approuvé avec réserves` (documentation
`roadmap/phase-7-bracket.md` obsolète après le changement de contrat de
`api_bracket_create`). Plutôt que de clore avec la réserve non résolue,
l'orchestrateur a corrigé directement la documentation (fichier local
`roadmap/`, non versionné) puis clos en ✅ — même pattern qu'en session #56
pour #229 : micro-correction documentée dans le commentaire GitHub plutôt
qu'un défaut trivialement corrigible laissé en l'état.

## Git
- Branche : `claude/sprint/18-tableau-final-conformite`
- Parent effectif : `claude/sprint/17-panneau-edition-match` (sprint 17 clos
  côté planification/milestone en session #57, mais sa PR #223 n'est pas
  encore mergée dans `main`). Branche créée depuis celle-ci cette session
  (nouvelle branche, aucun merge nécessaire).
- Push : ✅ succès

| Hash court | Message du commit |
|-----------|------------------|
| `d597dcf` | 18-196 ✅ Bracket : P3 incluse dans bracket-labels/assign/clear |
| `1d43600` | 18-197 ✅ Bracket : recréation forcée réelle (recreate_final_bracket_for_event) |
