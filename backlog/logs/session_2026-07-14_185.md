# Session 2026-07-14 — N°185

**Sprint actif :** 45 — Correctifs review globale du 13 juillet
**Tickets traités :** 2

## Spec Review

| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/screens/admin-shell.md` | ⚠️ Dérive mineure | 0 (correspond à #406, déjà ouverte) |
| `specs/screens/admin-tournoi.md` | ✅ Conforme | 0 |
| `specs/screens/admin-regie-mobile.md` | ⚠️ Dérive mineure ×2 | 0 (correspondent à #407, #409, déjà ouvertes — closes pendant cette session) |
| `specs/transverse/erreurs-api.md` | ⚠️ Dérive mineure | 0 (correspond à #411, déjà ouverte) |
| `specs/technical/planning.md` | ✅ Conforme | 0 |
| `specs/technical/cycle-de-vie-match.md` | ✅ Conforme | 0 |
| `specs/technical/cycle-de-vie-epreuve.md` | ✅ Conforme (note documentaire non bloquante) | 0 |

## Tickets clôturés

| # | Titre | Verdict |
|---|-------|---------|
| #409 | AdminRegie : score live épinglé sans les points (sets/jeux seulement) | ✅ Approuvé |
| #407 | AdminRegie : polling à taux unique 5s, pas 2s sur le match live épinglé | ✅ Approuvé |

## Nouveaux tickets créés

Aucun — 0 dérive surprise en spec review.

## Tickets à reprendre

Aucun.

## Fichiers partagés câblés

Aucun — `AdminRegie.vue` n'est pas un fichier réservé, tout le travail de cette session (spec review + les deux tickets) a été fait sans toucher aux fichiers partagés.

## Problèmes d'orchestration

Aucun écart au protocole. Spec review lancée strictement avant toute implémentation (correction du point d'attention noté en session #184, où elle avait été lancée en parallèle par souci de parallélisation — cette fois, séquencement strict respecté de bout en bout, sans confusion possible).

## Détail des tickets

**#409** (mineure) — `frontend/app/src/views/admin/AdminRegie.vue::scoreDisplay` (ligne 128) : le segment jeux en cours d'un match `LIVE` inclut désormais les points (`displayPointA`/`displayPointB`), ex. `6-4 3-2 (3-2, 30-15)` — conforme à `admin-regie-mobile.md` (« sets, jeux, points »). Aucun champ à ajouter côté contrat, `displayPointA`/`displayPointB` déjà exposés par `_pack_match` et déjà typés. Plan : `backlog/plan/409-adminregie-score-points.md`. Vérifié en preview (serveurs `django-backend`/`vite-frontend` démarrés via `.claude/launch.json`, match live réel id 53 de la donnée de seed) : la carte live affiche bien `1-6 7-5 3-6 (3-6, 0-0)`. Reviewer indépendant (en arrière-plan) : diff conforme, `FINISHED` non affecté, `vue-tsc` 0 erreur revérifié. Verdict : ✅ Approuvé, aucune réserve.

**#407** (mineure) — `frontend/app/src/views/admin/AdminRegie.vue` : ajout d'un second `usePolling(..., 2000)` dédié (lignes 43-59) qui rafraîchit uniquement le score du match live épinglé via `GET /api/matches/<id>/` (endpoint existant `api_match_detail`, réutilise `_pack_match`, même route que `ArbitreMatch.vue`/`stores/live.ts::fetchMatch`) — le poll calendrier existant (5s) reste seul responsable de la détection d'un *nouveau* match live, conforme au texte exact de la spec (« ~2s sur le match live, ~5s sur le fil de journée »). La carte live consomme `scoreDisplay(liveScoreData ?? liveMatch)`. Plan : `backlog/plan/407-adminregie-polling-2s-live.md`. Reviewer indépendant (en arrière-plan) : a confirmé que le placement du `usePolling(...)` avant la déclaration de `liveMatch` dans le fichier n'est pas un bug d'ordre (lecture directe de `usePolling.ts` : `fn()` n'est invoqué qu'à `onMounted`, après l'évaluation complète du `<script setup>`), aucune régression sur `openSheet(liveMatch)` (reste sur les données calendrier). A signalé une réserve **dans le périmètre** du ticket : `liveScoreData` n'était réinitialisé que si l'id du match live devenait `null`, pas s'il changeait d'une valeur à une autre (fenêtre transitoire ≤2s où le nom du nouveau match live pourrait s'afficher avec le score de l'ancien) — corrigée par l'orchestrateur avant clôture (garde `id !== liveScoreData.value?.id` ajoutée, même pattern que `stores/live.ts:89-91`), `vue-tsc` revérifié (0 erreur) et rendu en preview revérifié après correction. Verdict final : ✅ Approuvé.

## Point d'attention outillage — vérification navigateur

`.claude/launch.json` toujours disponible ; les deux serveurs ont été démarrés et le rendu réel confirmé par capture d'écran sur `/admin/regie` avec un match `LIVE` de la donnée de seed (id 53). En revanche, la vérification empirique du **taux** de polling à 2s n'a pas pu être faite : `document.hidden` vaut `true` dans cet environnement de preview (tab non focus), ce qui met en pause `usePolling` par design (comportement documenté, cohérent avec le composable — pas un bug). Compensé par : lecture de code, endpoint `/api/matches/53/` appelé directement en JS et vérifié retourner la forme attendue, et revue indépendante du composable par l'agent `reviewer`. À noter pour une session future disposant d'un environnement de preview avec tab visible si une vérification empirique du taux exact est jugée nécessaire.

## Git

- Branche : `claude/sprint/45-review-globale-13-juillet`
- Parent effectif : `claude/sprint/44-retours-12-juillet` (résolu par l'algorithme de l'Étape 0, inchangé depuis les sessions #179-#184 ; `git merge` no-op, déjà à jour)
- Push : ✅ succès

| Hash court | Message du commit |
|-----------|------------------|
| `c942d48` | sprint-45 📝 Spec review session #185 : 0 dérive surprise, 4/4 issues confirmées scopées |
| `6e13149` | 45-409 ✅ AdminRegie : score live épinglé — ajout des points au segment jeux |
| `337b93d` | 45-407 ✅ AdminRegie : poll dédié 2s sur le score du match live épinglé |
