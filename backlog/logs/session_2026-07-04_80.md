# Session 2026-07-04 — N°80

**Sprint actif :** 21 — Durcissements API & specs
**Tickets traités :** 2

## Spec Review

| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/screens/admin-tournoi.md` | ⚠️ Dérive mineure | 1 nouvelle : bouton « Sélectionner » décrit dans la spec, retiré du code (commit `4ed55bc`, issue #208, sprint 20) |
| `specs/technical/planning.md` | ✅ Conforme | Réconciliation #248 confirmée (5 endpoints vérifiés aux emplacements cités) ; #249 et #250 toujours valides tels que décrits (non re-signalés) |
| `specs/technical/cycle-de-vie-epreuve.md` | ✅ Conforme | Aucun commit sur les fichiers cités depuis la dernière review (#79) |
| `specs/technical/cycle-de-vie-match.md` | ✅ Conforme | Aucun commit sur les fichiers cités depuis la dernière review (#79) |

Review effectuée par un agent `reviewer` dédié, en lecture seule, indépendamment
de la review précédente (session #79). `npx vue-tsc --noEmit` : OK.

## Tickets clôturés

| # | Titre | Verdict |
|---|-------|---------|
| #216 | Front : durée de match par défaut sans UI (modale Édition) | ✅ Approuvé |
| #249 | CLAUDE.md : polling calendrier documenté ~5 s mais code à 2 s | ✅ Approuvé |

**#216** : ajout d'un champ « Durée de match par défaut (min) » dans
`EditionModal.vue` (borné ≥ 1, défaut 27, cohérent avec le back
`core/models.py:10` / `live/api_views.py:1260-1275`), câblage du payload
(`EditionPayload.default_match_duration_min` dans `event.ts`, fichier partagé
câblé par l'orchestrateur), documentation ajoutée dans
`specs/screens/admin-tournoi.md`. Points mineurs non bloquants notés par le
reviewer (pas de retour visuel si valeur invalide côté front ; clamp
silencieux plutôt que 400 côté back, pré-existant, hors périmètre).

**#249** : `CLAUDE.md` (§2 ligne 38, §4 lignes 83-84) corrigé — le calendrier
admin (`AdminMatches.vue:33`, 2000 ms) était classé à tort dans le groupe
~5 s ; il est maintenant dans le groupe ~2 s avec arbitre/scoreboard.
Corrigé au passage : la mention « cible ~4 s bracket ... encore à appliquer »
de la ligne 38, devenue incohérente avec le §4 qui documente déjà cette
migration comme faite sur TvBracket/AdminBracket (poules TV restant à ~5 s).
Tous les intervalles réels revérifiés par grep + lecture de fichier avant
correction.

## Nouveaux tickets créés

| # | Titre | Sévérité |
|---|-------|----------|
| #251 | Specs : admin-tournoi.md décrit encore le bouton « Sélectionner » retiré | 🟡 Mineure |

## Tickets à reprendre

Aucun (aucun verdict ❌ cette session).

**Tickets sprint-21 laissés de côté volontairement** (comme lors des sessions
précédentes) :
- **#215** et **#219** : le body de chacun exige explicitement une « décision
  produit d'abord » — réservé à une session humaine.
- **#250** : « à trancher » entre hors-scope assumé ou mémorisation de l'ETA
  entre polls — jugement de conception similaire, laissé à une session
  humaine plutôt que tranché de façon autonome.
- **#251** (créé cette session) : nécessitera une session future pour la
  réconciliation de spec (pas traité dans la même session que sa création,
  cohérent avec la limite de 2 tickets/session).

## Fichiers partagés câblés

- `frontend/app/src/stores/event.ts` : ajout de
  `default_match_duration_min?: number` à l'interface `EditionPayload`
  (#216), câblé directement par l'orchestrateur avant de déléguer
  l'implémentation de `EditionModal.vue` à l'agent `vue-screen`.

## Problèmes d'orchestration

- Écart mineur au séquencement strict : la planification/implémentation de
  #249 (correction `CLAUDE.md`) a été faite directement par l'orchestrateur
  (sans agent dédié) en parallèle de l'implémentation de #216 par l'agent
  `vue-screen`, plutôt que strictement l'un après l'autre. Justifié par la
  nature triviale et non ambiguë du changement (2 lignes de doc, vérifiées
  par grep). Les reviews, elles, ont bien été faites par des agents distincts
  en lecture seule pour chaque ticket avant clôture.
- Erreur de manipulation git : un premier `git add -A` a groupé par erreur
  les fichiers de #216 et #249 dans un seul commit ; corrigé par
  `git reset --soft` (aucun push effectué avant correction, aucune perte de
  travail) et recommit proprement en deux commits séparés, un par ticket.

## Git

- Branche : `claude/sprint/21-durcissements-api-specs`
- Parent effectif : `claude/sprint/20-transverse-erreurs-routing` (sprint 20
  toujours non mergé dans `main` — point récurrent, cf. sessions précédentes,
  à traiter côté humain). La branche du sprint 21 contient déjà ce parent
  (vérifié via `git merge-base --is-ancestor`), aucun merge nécessaire cette
  session.
- Working tree propre au démarrage, pas de conflit de merge.
- Push : ✅ succès

| Hash court | Message du commit |
|-----------|------------------|
| `07298fa` | 21-216 ✅ EditionModal : champ durée de match par défaut ajouté |
| `3d7d085` | 21-249 ✅ CLAUDE.md : cadence polling calendrier corrigée (2s, pas 5s) |

Issue GitHub créée : [#251](https://github.com/sirius911/moutilloux/issues/251).
