# Session 2026-07-08 — N°112

**Sprint actif :** 27 — Poules : suivi & suppression
**Tickets traités :** 2

## Spec Review

| Spec | Verdict | Nouvelles dérives |
|------|---------|--------------------|
| `admin-poules.md` | ❌ Dérive bloquante (attendu, 2/5 tickets seulement traités jusqu'ici en entrée de session) | 3 dérives confirmées, toutes déjà ticketées (#293, #295, #296) — 0 nouvelle issue |

## Tickets clôturés

| # | Titre | Verdict |
|---|-------|---------|
| #293 | Front : AdminGroups — action « Supprimer la poule » (INSCRIPTION) | ✅ Approuvé |
| #295 | Front : AdminGroups — mode suivi (classement V/D/Pts + résultats) dès EN_COURS | ✅ Approuvé |

## Nouveaux tickets créés

Aucun.

## Tickets à reprendre

| # | Titre | Raison |
|---|-------|--------|
| #296 | Front : AdminGroups — légende du badge Q et des états | Mineure, non traitée cette session (max 2 tickets/session déjà atteint avec #293+#295) |

## Fichiers partagés câblés

- `frontend/app/src/stores/event.ts` : action `deleteGroup(groupId, eventId)` ajoutée (POST `/api/groups/<id>/delete/` + refetch groups/players), pour #293. Aucune autre modification de fichier partagé cette session (`types/index.ts` n'est pas dans la liste réservée — édité directement par l'agent pour #295 : ajout de `Group.matches: Match[]`).

## Problèmes d'orchestration

Aucun. Écart mineur au plan sur #295 (agent a utilisé `Entry.displayName`
au lieu de `player.fullName` suggéré, pour couvrir aussi les matchs de
double) — justifié et validé en review, noté dans le commentaire de
clôture de l'issue.

## Git

- Branche : `claude/sprint/27-poules-suivi-suppression`
- Parent effectif : `claude/sprint/26-qualification-poule-terminee` (sprint 26
  toujours non mergé dans `origin/main` — même mécanique de résolution que
  la session précédente)
- Push : ✅ succès

| Hash court | Message du commit |
|-----------|---------------------|
| `07d1a43` | 27-293 ✅ Front : AdminGroups — action Supprimer la poule |
| `9fa9e5e` | 27-295 ✅ Front : AdminGroups — mode suivi classement + matchs |

## Point d'attention outillage

Toujours pas de script `type-check` dédié dans `package.json` — `npx
vue-tsc --noEmit` utilisé directement (2× cette session, aucune erreur).
Pas de configuration `.claude/launch.json` pour prévisualiser l'app dans ce
repo — vérification faite par type-check + revue de code, cohérent avec les
sessions précédentes du moteur automatisé (pas de QA navigateur en session
non supervisée).

## Sprint 19/20/21 — PRs non mergées

Toujours d'actualité (PR #223/#232/#239/#246/#247, chaîne empilée depuis le
sprint 06 non fusionnée dans `main`). Hors périmètre de la Routine
automatique — nécessite une revue/merge humaine.
