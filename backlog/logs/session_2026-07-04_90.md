# Session 2026-07-04 — N°90

**Sprint actif :** 22 — État TV : contrat back
**Tickets traités :** 2

## Spec Review
| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/technical/tv-state.md` | ❌ Dérive bloquante (attendu) | 0 — `tv/state`/`tv/idle` sont câblés et `tv/idle` conforme, mais au moment de la review (début de session) `_pack_tv_stake` portait encore le bug `hero.event.name` (#253, à-reprendre) et le CRUD Announcement (#256) n'était pas codé ; côté front, `stores/live.ts`/`types/index.ts` restent sur le legacy (`fetchScoreState`/`fetchUpcoming`) — attendu, retraits prévus au sprint 23 |

## Tickets clôturés
| # | Titre | Verdict |
|---|-------|---------|
| #253 | `GET /api/tv/state/` — hero `_pack_match` + enjeu (stake) + next unifié | ✅ Approuvé (reprise) |
| #256 | CRUD Announcement (services + endpoints fins) | ✅ Approuvé |

## Nouveaux tickets créés
| # | Titre | Sévérité |
|---|-------|----------|
| — | — | — |

## Tickets à reprendre
| # | Titre | Raison |
|---|-------|--------|
| — | — | — |

## Fichiers partagés câblés
- `live/urls.py` : 4 routes CRUD Announcement (`api/editions/<id>/announcements/`, `.../announcements/create/`, `api/announcements/<id>/edit/`, `api/announcements/<id>/delete/`) pour #256.

## Problèmes d'orchestration

Aucun. Note technique : #253 (fix ciblé de 2 lignes) et #256 (nouvelles fonctions
en fin de fichier) ont été traités par deux agents en parallèle sur les mêmes
fichiers (`live/api_views.py` notamment) — zones disjointes, aucun conflit,
mais le commit de #253 a nécessité un patch partiel (`git apply --cached`)
pour isoler ses deux lignes du reste du diff avant de committer #256
séparément.

## Git
- Branche : `claude/sprint/22-tv-state-back`
- Parent effectif : `claude/sprint/21-durcissements-api-specs` (résolu via
  `backlog/sprints/done/` — toujours pas mergée dans `origin/main` ; merge
  sans conflit : already up to date)
- Push : ✅ succès

| Hash court | Message du commit |
|-----------|------------------|
| `32f2601` | 22-253 ✅ Back : fix _pack_tv_stake hero.event.name → category.name |
| `c27674c` | infra: spec review session #90 — tv-state.md, 0 nouvelle dérive |
| `8d1a85f` | 22-256 ✅ Back : CRUD Announcement (services + endpoints) |
