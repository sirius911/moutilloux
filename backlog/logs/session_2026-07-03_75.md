# Session 2026-07-03 — N°75

**Sprint actif :** 20 — Transverse : erreurs API & routing
**Tickets traités :** 2 (3 issues fermées)

## Spec Review
| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/technical/routing-context.md` | ⚠️ Dérive mineure | 0 nouvelle (confirmation de #208, `AdminTournoi.vue:229`) |
| `specs/screens/admin-shell.md` | ⚠️ Dérive mineure | 0 nouvelle (confirmation de #209, `AdminLayout.vue:56-59` — portée un peu plus large que le libellé initial : 4 compteurs sur 6 touchés) |

Aucun lien wiki mort résiduel (vérification de tous les `[[...]]` des deux specs).
`npx vue-tsc --noEmit` OK avant traitement des tickets.

## Tickets clôturés
| # | Titre | Verdict |
|---|-------|---------|
| #208 | « Sélectionner » (Tournoi) mute le store | ✅ Approuvé |
| #177 | GenerateMatchesModal.vue orphelin | ✅ Approuvé |
| #210 | CategoryModal / CourtModal orphelins | ✅ Approuvé |

**#208** : suppression du bouton « Sélectionner »/« Sélectionnée ✓ » dans
`AdminTournoi.vue` (mutait `eventStore.activeEventId` directement). Décision
retenue parmi les deux options de l'issue : retrait plutôt que navigation, car
les 3 boutons Inscriptions/Poules/Matchs de la même carte naviguent déjà
explicitement avec `:eventId` — la sélection y est déjà implicite. Le « vrai »
sélecteur d'épreuve en en-tête (décision 16) avait déjà été corrigé par un
plan antérieur (117). Aucun fichier partagé touché (`stores/event.ts`
inchangé). Reviewer indépendant : ✅ Approuvé, aucune mutation directe
résiduelle de `activeEventId` ailleurs dans le repo hors du watcher de route
prescrit par la spec.

**#177 + #210** (même geste, fichiers disjoints, embarqués ensemble comme
prévu par `sprint.md`) : suppression des 3 fichiers de modale orphelins —
`GenerateMatchesModal.vue`, `CategoryModal.vue`, `CourtModal.vue` (aucune
référence dans le code, confirmé par grep). Mise à jour du champ `fichiers:`
de l'en-tête YAML de `specs/screens/admin-matchs.md` (retrait de la ligne
`GenerateMatchesModal.vue`, contenu de la spec inchangé). Reviewer
indépendant : ⚠️ Approuvé avec réserves — la seule réserve porte sur le
working tree partagé entre les deux tickets traités dans la même session
(travail de #208 encore non commité au moment de la revue de #177/#210), pas
sur le contenu de la livraison elle-même ; corrigé en séparant les commits par
ticket.

`vue-tsc --noEmit` OK après chaque ticket.

## Nouveaux tickets créés
| # | Titre | Sévérité |
|---|-------|----------|
| — | — | — |

## Tickets à reprendre
| # | Titre | Raison |
|---|-------|--------|
| — | — | — |

## Fichiers partagés câblés
- Aucun cette session (#208 résolu sans toucher `stores/event.ts`).

## Problèmes d'orchestration
- Le plan de #177+#210 a été revu alors que le commit de #208 n'était pas
  encore posé : le reviewer a vu un working tree mêlant les deux tickets
  (réserve non bloquante). Correction : commits désormais séparés par ticket
  (`4ed55bc` pour #208, `32dd110` pour #177+#210). À l'avenir, committer
  chaque ticket avant de lancer la revue du suivant.

## Git
- Branche : `claude/sprint/20-transverse-erreurs-routing`
- Parent effectif : `claude/sprint/19-poules-inscriptions-ajustements` (sprint 19 toujours non mergé dans `main`)
- Push : ✅ succès

| Hash court | Message du commit |
|-----------|------------------|
| `4ed55bc` | 20-208 ✅ AdminTournoi : retrait du bouton Sélectionner (mutation directe du store) |
| `32dd110` | 20-177 20-210 ✅ Suppression des modales orphelines (GenerateMatches/Category/Court) |
| `0995641` | 20 📋 Log spec review session #75 (routing-context, admin-shell) |
