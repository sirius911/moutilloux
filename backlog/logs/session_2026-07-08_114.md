# Session 2026-07-08 — N°114

**Sprint actif :** 27 — Poules : suivi & suppression (4ᵉ et dernière session du sprint)

**Tickets traités :** 0 (les 5 tickets du sprint — #292 à #296 — étaient déjà tous clos en entrée de session)

## Spec Review

| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `admin-poules.md` | ✅ Conforme | 0 |

Réserve mineure déjà connue (session #113) confirmée non bloquante : la pastille de
légende « Forfait » utilise `--danger` (rouge) alors que la ligne de match walkover
reste visuellement `gc-match--finished` (vert, `--success`) dans la grille — seul le
libellé texte change (`AdminGroups.vue:902,970-971`). La spec ne précise que le
libellé texte, sans exiger de distinction chromatique : conforme au texte strict,
non actionnable, laissé en l'état.

Tous les autres points vérifiés `✅` : suppression de poule en `INSCRIPTION` (#293),
refus serveur si `EN_COURS` (#292), endpoint `POST /api/groups/<id>/delete/` sans
duplication de logique métier, mode suivi classement + matchs dès `EN_COURS` (#295),
légende + tooltip du badge Q (#296), `api_event_groups`/`_pack_match` par poule (#294),
contrat de types, garde de rôle `requiresAdmin`, composables `useApi`/`usePolling`
(polling ~5 s actif seulement en mode suivi), CSS/tokens. `npx vue-tsc --noEmit` :
0 erreur.

## Tickets clôturés

| # | Titre | Verdict |
|---|-------|---------|

_Aucun — tous les tickets du sprint avaient déjà été traités lors des sessions
#111/#112/#113._

## Nouveaux tickets créés

| # | Titre | Sévérité |
|---|-------|----------|

_Aucun._

## Tickets à reprendre

| # | Titre | Raison |
|---|-------|--------|

_Aucun._

## Fichiers partagés câblés

Aucun cette session.

## Problèmes d'orchestration

Aucun.

## Sprint 27 — clôturé

Les deux critères de l'étape 3 sont réunis dans cette session : spec review
`✅ Conforme` sur `admin-poules.md`, 0 issue `sprint-27` ouverte.

- Milestone GitHub « Sprint 27 — Poules : suivi & suppression » (n°26) fermé.
- Ligne supprimée de `backlog/sprints/roadmap.md`.
- Dossier déplacé : `backlog/sprints/27-poules-suivi-suppression/` →
  `backlog/sprints/done/27-poules-suivi-suppression/`.
- Roadmap relue : il reste 5 sprints (28 à 32). Le sprint 28 — Admin : annonces
  & inscription par sélection — devient le sprint actif, mais **ne sera traité qu'à
  la prochaine échéance planifiée**, pas dans cette session.

## Git

- Branche : `claude/sprint/27-poules-suivi-suppression`
- Parent effectif : `claude/sprint/26-qualification-poule-terminee` (toujours non
  mergé dans `origin/main` ; `git merge origin/<parent>` déjà à jour, rien à intégrer)
- Push : voir commit ci-dessous

| Hash court | Message du commit |
|-----------|------------------|
| `d88677c` | infra: clôture sprint 27 — spec ✅ conforme, milestone fermé (#114) |
