# Session 2026-07-07 — N°102

**Sprint actif :** 24 — Affiches de match (clos en fin de session)
**Tickets traités :** 1

## Spec Review

| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/technical/affiche-match.md` | ✅ Conforme | 0 (dérive #287 confirmée toujours présente, déjà ticketée — traitée cette session) |
| `specs/screens/admin-joueurs.md` | ✅ Conforme | 0 |
| `specs/screens/admin-matchs.md` | ✅ Conforme | 0 (dérive #287 déjà connue, non re-signalée) |
| `specs/screens/tv-live.md` | ✅ Conforme | 0 |

Deux suggestions annexes hors-périmètre relevées par la review (non ticketées,
déjà couvertes par un TODO général) : redirection 401 non gérée sur le
mini-fetch multipart d'`AddPlayerModal.vue` (limitation connue de `useApi`,
documentée CLAUDE.md §4), et absence de `transaction.atomic()` sur
`select_poster_candidate`/`_purge_job` (`live/posters.py`).

## Tickets clôturés

| # | Titre | Verdict |
|---|-------|---------|
| #287 | EditMatchPanel : bouton « Générer » n'anticipe pas la garde photo manquante | ✅ Approuvé |

Fix : `PosterSlot` étendu avec `photoUrl`, `buildPosterSlots` le peuple pour
Simple (`side.player.photoUrl`) et Double (`side.team.player1/2.photoUrl`,
réutilise la résolution de #286), nouveau computed `missingPhotoNames`,
`generateDisabledReason` retourne un message citant le(s) nom(s) manquant(s)
avant l'appel serveur. Seul fichier modifié :
`frontend/app/src/components/modals/EditMatchPanel.vue`. `npx vue-tsc
--noEmit` sans erreur, aucune régression sur le golden path Simple.

## Nouveaux tickets créés

Aucun.

## Tickets à reprendre

Aucun.

## Fichiers partagés câblés

Aucun (ticket #287 ne touchait qu'un fichier non partagé).

## Problèmes d'orchestration

Aucun. Protocole suivi à la lettre cette session : planification (2a) rédigée
directement par l'orchestrateur (contexte d'investigation déjà en main pendant
l'attente de la review de specs), implémentation (2b) et review (2c) déléguées
à des agents dédiés (`vue-screen`, `reviewer`).

## Fin de sprint

**Sprint 24 terminé** — les deux conditions de clôture sont réunies : spec
review de cette session `✅ Conforme` sur les 4 specs, et 0 issue ouverte avec
le milestone « Sprint 24 — Affiches de match » (hors `en-attente`).

- Milestone « Sprint 24 — Affiches de match » (n°23) fermé via l'API GitHub.
- PR #1 (« ADD Script Generate_match_poster.py ») fermée avec le commentaire
  « Intégrée par le sprint 24 », conformément à `sprint.md`.
- Ligne du sprint 24 supprimée de `backlog/sprints/roadmap.md`.
- Dossier déplacé : `backlog/sprints/24-affiches-match/` →
  `backlog/sprints/done/24-affiches-match/`.

**Roadmap restante :** 1 sprint — 25 — Arbitre : fins spéciales & résilience.
Il sera traité à la **prochaine échéance planifiée**, pas dans cette même
session (règle du protocole).

## Git

- Branche : `claude/sprint/24-affiches-match`
- Parent effectif : `claude/sprint/23-tv-live-front` (résolu via
  `backlog/sprints/done/` — sprint 23 toujours pas mergé dans `origin/main`
  malgré `branche-parent: main` dans le frontmatter du sprint 24, conforme à
  la règle « la roadmap prime sur le frontmatter »)
- Merge avec le parent : déjà à jour (`Already up to date.`)
- Push : voir ci-dessous

| Hash court | Message du commit |
|-----------|------------------|
| `4d58ed7` | 24-287 ✅ EditMatchPanel : garde photo manquante anticipée sur le bouton Générer |

(le commit du déplacement du dossier sprint + roadmap suit dans cette même session)
