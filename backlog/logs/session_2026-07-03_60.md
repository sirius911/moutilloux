# Session 2026-07-03 — N°60

**Sprint actif :** 18 — Tableau final conforme (3ᵉ session).
**Tickets traités :** 2 — #200, #199.

## Spec Review
| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/screens/admin-tableau-final.md` | ⚠️ Dérive mineure | 0 nouvelle — 5 dérives déjà connues confirmées (#199, #200, #176, #230, #231) |

Les corrections des sessions #58/#59 tiennent sans régression : sélecteur
d'épreuve navigant, polling 4s sans double-fetch, P3 incluse partout,
recréation forcée toujours gardée côté serveur.

## Tickets clôturés
| # | Titre | Verdict |
|---|-------|---------|
| [#200](https://github.com/sirius911/moutilloux/issues/200) | Front : AdminBracket propose « Créer le tableau » en INSCRIPTION au lieu d'inviter à débuter | ✅ Approuvé |
| [#199](https://github.com/sirius911/moutilloux/issues/199) | Front : recréation du tableau via confirm() natif au lieu d'une modale | ✅ Approuvé (avec réserve corrigée avant clôture) |

### Détail #200
`AdminBracket.vue` : nouveau bloc `v-if="activeEvent?.status === 'INSCRIPTION'"`
(état vide invitant à débuter, lien vers `/admin/tournoi`) prioritaire sur le
bloc `!hasBracket` existant (conservé comme filet de sécurité pour EN_COURS
sans bracket). Pattern identique à `AdminMatches.vue`. Seul ce fichier
modifié. `npx vue-tsc --noEmit` OK.

### Détail #199
`AdminBracket.vue` : `window.confirm()` remplacé par une `ConfirmModal`
dédiée (warning conforme au texte exact de la spec, `:danger="true"`). La
création initiale reste sans confirmation. En cas d'échec serveur (garde
LIVE/FINISHED), la modale reste ouverte et affiche l'erreur — `ConfirmModal`
n'ayant pas de slot dédié et n'étant pas modifiable dans ce ticket (composant
partagé), le message est composé en texte dans le `body`. Le reviewer a
rendu ⚠️ Approuvé avec réserves (manque de séparation visuelle entre le
warning et l'erreur) — corrigé par l'orchestrateur en préfixant l'erreur par
« Erreur : » pour plus de clarté, sans toucher au composant partagé.
`npx vue-tsc --noEmit` OK.

## Nouveaux tickets créés
| # | Titre | Sévérité |
|---|-------|----------|
| — | — | — |

## Tickets à reprendre
| # | Titre | Raison |
|---|-------|--------|
| — | — | — |

## Fichiers partagés câblés
- Aucun. `ConfirmModal.vue`, `stores/event.ts`, `router/index.ts` non touchés.

## Problèmes d'orchestration
Pour #199, le reviewer a rendu `⚠️ Approuvé avec réserves` (message d'erreur
serveur concaténé au warning sans séparation visuelle dans la modale).
Plutôt que clore avec la réserve non résolue, l'orchestrateur a appliqué un
ajustement textuel d'une ligne (préfixe « Erreur : ») dans `AdminBracket.vue`
uniquement, sans modifier le composant partagé `ConfirmModal.vue`, revérifié
le type-check, puis clos en ✅ — même pattern qu'en sessions #56, #58, #59.
Aucun autre écart au protocole.

## Git
- Branche : `claude/sprint/18-tableau-final-conformite`
- Parent effectif : `claude/sprint/17-panneau-edition-match` (sprint 17 clos
  côté planification/milestone depuis la session #57, mais sa PR #223 n'est
  toujours pas mergée dans `main`). Déjà synchronisé cette session, aucun
  merge nécessaire.
- Push : ✅ succès

| Hash court | Message du commit |
|-----------|------------------|
| `6325678` | 18-200 ✅ AdminBracket : état INSCRIPTION invite à débuter (pas de création manuelle) |
| `59d2f67` | 18-199 ✅ AdminBracket : recréation via ConfirmModal dédiée (plus de confirm() natif) |
