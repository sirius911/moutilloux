# Session 2026-07-14 — N°182

**Sprint actif :** 45 — Correctifs review globale du 13 juillet
**Tickets traités :** 2

## Spec Review
| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/screens/admin-shell.md` | ⚠️ | 0 (#406 reconfirmée) |
| `specs/screens/admin-tournoi.md` | ⚠️ | 0 (#395, #396 reconfirmées) |
| `specs/screens/admin-regie-mobile.md` | ⚠️ | 1 (#409 — score live épinglé sans les points) |
| `specs/transverse/erreurs-api.md` | ⚠️ | 0 (#405 reconfirmée) |
| `specs/technical/planning.md` | ⚠️ | 1 (#410 — `_pack_match` repli sans `~`) |
| `specs/technical/cycle-de-vie-match.md` | ✅ | 0 |
| `specs/technical/cycle-de-vie-epreuve.md` | ⚠️ | 0 (#400 reconfirmée) |

## Tickets clôturés
| # | Titre | Verdict |
|---|-------|---------|
| #395 | Announcement — nouvelle annonce en tête de liste | ✅ Approuvé (après correction d'une réserve) |
| #396 | AdminTournoi — dates d'édition au format français | ✅ Approuvé (après correction d'une réserve) |

## Nouveaux tickets créés
| # | Titre | Sévérité |
|---|-------|----------|
| #409 | AdminRegie — score live épinglé sans les points | 🟠 mineure |
| #410 | `_pack_match` — repli sans le préfixe `~` quand `eta_display` absent | 🟡 mineure |

## Tickets à reprendre
_Aucun._

## Fichiers partagés câblés
_Aucun cette session — aucun des deux tickets ne touchait un fichier partagé._

## Problèmes d'orchestration
Aucun écart au protocole. Les deux agents `reviewer` (spec review + review de
tickets) ont chacun signalé un problème hors périmètre au lieu de le
corriger eux-mêmes :
- la spec review a trouvé 2 dérives surprises non ticketées (#409, #410) ;
- la review de tickets a trouvé une réserve **sur le périmètre même** des
  deux tickets (pas hors périmètre cette fois) : `api_tv_idle` avait un
  `order_by("created_at")` explicite qui écrasait le nouvel ordre du modèle
  pour #395, et le format de date de #396 ne zéro-préfixait pas le jour.
  Les deux réserves ont été corrigées par l'orchestrateur avant clôture,
  puis re-vérifiées (`manage.py check`, `vue-tsc --noEmit`, test Node du
  format de date) — pas de second passage par l'agent `reviewer`, verdict
  final assumé par l'orchestrateur sur la base du diagnostic précis du
  reviewer.

## Git
- Branche : `claude/sprint/45-review-globale-13-juillet`
- Parent effectif : `claude/sprint/44-retours-12-juillet` (résolu par
  l'algorithme de l'Étape 0, inchangé depuis les sessions #179-#181)
- `git merge origin/claude/sprint/44-retours-12-juillet --no-edit` : déjà à
  jour, aucun commit de rattrapage nécessaire (working tree propre au
  démarrage)
- Push : en cours (voir note en fin de log)

| Hash court | Message du commit |
|-----------|------------------|
| `f7aa0dd` | sprint-45 📝 Log spec review #182 (1 ✅, 6 ⚠️, 2 nouvelles dérives → #409/#410) |
| `3a05b4c` | 45-395 ✅ Announcement : tri -created_at (admin + TV idle) |
| `3e1b070` | 45-396 ✅ AdminTournoi : dates d'édition au format français |

## Détail

**Spec review session #182** (agent `reviewer` dédié, lecture seule, en tout
début de session) : 7 specs ciblées, 1 ✅ Conforme (`cycle-de-vie-match.md`,
transition Terminé → Prévu de #403 reconfirmée), 6 ⚠️ Dérive mineure. 5 des 7
dérives correspondent exactement aux 5 issues déjà ouvertes (`#395`, `#396`,
`#400`, `#407`, `#408`). **2 dérives nouvelles et surprises :**
- `AdminRegie.vue:108-112` (`scoreDisplay`) — le score du match live épinglé
  n'affiche que sets + jeux, jamais les points (`displayPointA`/
  `displayPointB`), contrairement à `admin-regie-mobile.md` et au plan
  d'implémentation `backlog/plan/340-adminregie-ecran-complet.md:127-129` qui
  le redemandent explicitement. Même lacune constatée sur `ArbitreHome.vue`
  (hors périmètre du sprint 45, non ticketée). → `#409`.
- `live/api_views.py:242-243` (`_pack_match`, branche de repli sans
  `eta_display`) — renvoie l'heure sans le préfixe `~` canonique établi par
  #398. Impact nul aujourd'hui (les endpoints concernés voient leur réponse
  `match` ignorée côté client, qui refetch le calendrier), mais piège latent
  pour un futur écran. → `#410`.

Régression #398/#402 (session #181) : aucune détectée — grep de contrôle
propre sur les 6 fichiers du sweep #398, parsing `AdminRegie.vue:155` (#402)
toujours en place. `npx vue-tsc --noEmit` vérifié indépendamment (0 erreur).

**Backlog engine session #182** : 2 tickets traités séquentiellement (aucune
issue `à-reprendre`, toutes mineures), dans l'ordre suggéré par la note de
fin de session #181 (`#395`/`#396` — indépendants, triviaux), chacun
implémenté **directement en session par l'orchestrateur** (portée triviale,
un seul fichier de code par ticket), soumis à un agent `reviewer`
indépendant avant clôture :
- **#395** (mineure) — `live/models.py:303` :
  `Announcement.Meta.ordering` passé de `["created_at"]` à
  `["-created_at"]` + migration `0025_alter_announcement_options.py`
  (`AlterModelOptions` pur, pas de changement de schéma). Plan :
  `backlog/plan/395-announcement-ordering-tete-liste.md`. Le reviewer a
  trouvé le correctif **incomplet** : `live/api_views.py:2426`
  (`api_tv_idle`, endpoint qui alimente la slide TV et le ticker via
  `stores/live.ts`) portait un `.order_by("created_at")` explicite qui
  écrasait le nouvel ordre du modèle — seules les vues admin
  (AdminTournoi, AdminRegie, qui ne forcent pas d'ordre) étaient
  effectivement corrigées. Corrigé par l'orchestrateur (`order_by` retiré,
  l'endpoint hérite désormais du `Meta.ordering`), reverdicté ✅ sans
  nouveau passage reviewer (correction mécanique d'une ligne, diagnostic du
  reviewer sans ambiguïté). `manage.py check` : 0 erreur.
- **#396** (mineure) — `frontend/app/src/views/admin/AdminTournoi.vue` :
  `fmtDate` (ISO brut `iso.slice(0,10)`) remplacée par
  `formatPeriod(startIso, endIso)` — format français, année élidée sur la
  date de début si même année civile que la date de fin. Plan :
  `backlog/plan/396-admintournoi-dates-francais.md`. Le reviewer a trouvé un
  écart à l'exemple exact de la spec (« 25 mai → 02 juin 2026 ») : l'option
  `day: 'numeric'` produisait `"2 juin 2026"` (non zéro-préfixé) au lieu de
  `"02 juin 2026"`. Corrigé par l'orchestrateur (`day: '2-digit'`),
  revérifié par un script Node isolé confirmant l'exemple exact de la spec.
  `npx vue-tsc --noEmit` : 0 erreur.

**Sprint 45 non clos cette session** (attendu, lecture stricte de l'Étape 3 —
la spec review a précédé l'implémentation et a donc trouvé 6 specs encore
⚠️, précédent constant depuis la session #161) : 7 issues encore ouvertes en
fin de session (`#400`, `#405`, `#406`, `#407`, `#408`, `#409`, `#410`).
Sprint reste actif, sera repris à la **prochaine échéance planifiée**. Ordre
suggéré : `#405` (sweep erreurs API résiduel, indépendant, 3 fichiers
triviaux) ; `#408` (indépendant, trivial, `AdminGroups.vue`, même patron que
`#398`/`AdminMatches.vue:537`) ; `#407` (indépendant, `AdminRegie.vue`
polling) ; `#409` (indépendant, `AdminRegie.vue` points manquants — même
fichier que #407, à coordonner en une passe) ; `#400` (petite finale sans
demies, back) ; `#410` (`_pack_match` repli sans `~`, back, trivial) ;
`#406` (fichier partagé `stores/event.ts`, réservé à l'orchestrateur).

**Point d'attention outillage :** `.claude/launch.json` toujours présent,
**pas utilisé cette session** — les deux tickets vérifiés par lecture de
code + `manage.py check` + `vue-tsc` + script Node isolé (format de date) +
`reviewer` indépendant, sans QA navigateur réelle. Portée jugée
suffisamment couverte (correctifs mécaniques : tri de queryset, formatage
de date) ; pas de régression attendue nécessitant une vérification
visuelle.

**Point d'attention protocole (reviewer) :** l'agent `reviewer` de la spec
review a signalé 2 dérives surprises hors périmètre plutôt que de les
corriger (`#409`, `#410`). L'agent `reviewer` de la review de tickets a, pour
la première fois depuis plusieurs sessions, trouvé des réserves **dans le
périmètre même** des tickets examinés (pas hors périmètre) — comportement
attendu et correct pour ce cas de figure (le mandat de lecture seule reste
respecté : il a signalé sans corriger). Pattern de lecture seule strict
stable sur au moins 32 sessions consécutives (#140-#182, sessions à vide
comprises) depuis l'incident initial de la session #139. Aucun
`ScheduleWakeup` utilisé.

Log complet : `backlog/logs/session_2026-07-14_182.md`.
