# Session 2026-07-12 — N°164

**Sprint actif :** 43 — Correctifs retours du 11 juillet
**Tickets traités :** 2

## Spec Review

| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/screens/tv-live.md` | ⚠️ Dérive mineure | 0 (dérives = #374, #379 — déjà ouvertes) |
| `specs/technical/tv-state.md` | ✅ Conforme | 0 |
| `specs/screens/arbitre-match.md` | ⚠️ Dérive mineure | 0 (dérive = #376 — déjà ouverte) |
| `specs/screens/admin-inscriptions.md` | ⚠️ Dérive mineure | 0 (dérives = #377/#378 — déjà ouvertes, #377 clos en fin de session) |
| `specs/technical/planning.md` | ⚠️ Dérive mineure | 0 (dérives = #375/#380 — déjà ouvertes) |

Review confiée à un agent `reviewer` dédié (lecture seule) : toutes les
dérives relevées sur les 5 specs ciblées correspondent exactement aux 8
issues qui étaient encore ouvertes en début de session (`#372`, `#374`–
`#380`), 0 dérive additionnelle surprenante, 0 nouvelle issue créée.
`tv-state.md` bascule à ✅ Conforme cette session (confirme #371/#373 clos
à la session #163).

## Tickets clôturés

| # | Titre | Verdict |
|---|-------|---------|
| #377 | Back : Double — refuser un joueur déjà engagé dans une équipe de l'épreuve | ✅ Approuvé |
| #372 | Front : TV — banderole d'information pendant le match | ✅ Approuvé |

## Nouveaux tickets créés

| # | Titre | Sévérité |
|---|-------|----------|

Aucun — toutes les dérives relevées par la spec review correspondaient à des
issues déjà ouvertes.

## Tickets à reprendre

| # | Titre | Raison |
|---|-------|--------|

Aucun.

## Fichiers partagés câblés

Aucun cette session — les deux tickets touchaient exclusivement des fichiers
hors périmètre orchestrateur (`live/admin_views.py` pour #377,
`TvTicker.vue` nouveau + `TvScoreboard.vue` pour #372, ni l'un ni l'autre
listés en §3/§2b de `CLAUDE.md`/`SESSION_ENGINE.md`).

Mise à jour de spec (agent de maintenance non nécessaire, fait directement
par l'orchestrateur — changement trivial d'un seul champ) :
`specs/screens/tv-live.md` — ajout de `frontend/app/src/views/tv/TvTicker.vue`
au champ `fichiers:` de l'en-tête YAML (contenu de la spec non touché).

## Problèmes d'orchestration

Aucun — protocole suivi normalement.

## Git

- Branche : `claude/sprint/43-retours-11-juillet`
- Parent effectif : `main` (`git merge-base --is-ancestor origin/main HEAD`
  positif d'emblée au démarrage, working tree propre, aucun merge ni commit
  de rattrapage nécessaire)
- Push : ✅ succès

| Hash court | Message du commit |
|-----------|------------------|
| `41d6b6a` | 43-377 ✅ Double : refuser un joueur déjà en équipe sur l'épreuve |
| `9e1d6ac` | 43-372 ✅ TV : banderole d'information pendant le match (TvTicker) |

## Détail des tickets

**#377** (majeure) — `live/admin_views.py` — en Double, un joueur déjà
inscrit dans une équipe pouvait se réinscrire dans une autre équipe de la
même épreuve : `create_team_with_entry` ne vérifiait que `player1 !=
player2`, aucune garde ne liait les joueurs des équipes déjà inscrites (et
la même lacune existait dans `add_late_entry` et `replace_entry_player`).
Traité **directement en session** (aucun agent `django-api` — portée trop
réduite pour justifier une extraction de service supplémentaire, la logique
vit déjà dans les fonctions de service existantes). Fix : nouvelle fonction
utilitaire `_find_player_in_other_team_entry(event, player1, player2,
exclude_entry_id=None)` (requête `Entry` avec `Q(team__player1__in=...) |
Q(team__player2__in=...)`, `Q` déjà importé), appelée dans les 3 points
d'entrée — `replace_entry_player` avec `exclude_entry_id=entry.pk` pour ne
pas se bloquer soi-même lors d'un remplacement partiel. Reviewer : logique
et golden path (A/B puis A/C même épreuve → 400 ; A/D autre épreuve →
accepté ; remplacement sans faux positif) retracés ligne à ligne,
`manage.py check` vérifié indépendamment (0 erreur), rétrocompatibilité des
3 appelants existants dans `api_views.py` confirmée (paramètre optionnel).
Verdict : ✅ Approuvé, aucune réserve.

**#372** (majeure) — `frontend/app/src/views/tv/TvTicker.vue` (nouveau) +
`TvScoreboard.vue` — nouvelle feature (aucune maquette `.jsx` de référence,
retour produit récent) : une banderole d'information ancrée en bas de la
scène TV, active pendant un match (SCOREBOARD **et** ÉCHAUFFEMENT — cette
dernière scène n'avait auparavant aucun pied de page), fait défiler en
continu les annonces actives, derniers résultats et prochains matchs
(données déjà pollées en continu depuis #371, clos session #163), avec un
segment fixe à gauche (court/durée/horloge) qui remplace l'ancien
`.sb-foot-discreet`. Confié à un agent `vue-screen` (nouveau composant +
édition étendue de `TvScoreboard.vue`, portée suffisante pour justifier
l'agent contrairement à #371/#373). Fix : `computed items` (annonces →
résultats → prochains matchs, formats de texte alignés sur ceux déjà
utilisés dans `TvIdle.vue`), piste doublée (`[...items, ...items]`) +
`translateX(0 → -50%)` en boucle linéaire infinie pour un bouclage sans
à-coup, durée d'animation proportionnelle au nombre d'items (14s–60s), cas
`items.length === 0` → seul le segment fixe reste visible. Géométrie :
bande à `bottom: 12px; height: 44px` (haut à 56px), sous `.sb-ed-bottom`
(`bottom: 72px`) avec 16px de marge de sécurité — vérifiée indépendamment
par le reviewer, ainsi que l'absence de conflit avec `.tv-prep`. Reviewer :
diff conforme au plan, `.sb-foot-discreet` proprement retiré (CSS + grep
global confirmant aucune autre référence), `npx vue-tsc --noEmit` vérifié
indépendamment (0 erreur). Verdict : ✅ Approuvé, aucune réserve — seul
point annexe non bloquant relevé : le champ `fichiers:` de
`specs/screens/tv-live.md` ne listait pas encore `TvTicker.vue`, corrigé
par l'orchestrateur juste après la review (changement trivial du champ YAML
uniquement, contenu de la spec non touché).

## Sprint 43 — état de fin de session

6 issues encore ouvertes sur le milestone : `#374`–`#376`, `#378`–`#380`.
Spec review encore ⚠️ sur 4 des 5 specs (attendu, le reste du sprint n'est
pas implémenté). Sprint reste actif, sera repris à la **prochaine échéance
planifiée**. Ordre suggéré par `sprint.md` pour la suite : `#374` (palmarès,
prérequis #371/#373 déjà clos) ∥ `#376` (arbitre) ∥ `#375` (ETA, gros
morceau back isolé) ; finitions `#378` (après #377, désormais clos) et
`#380` (après #375) en dernier ; `#379` (même zone que #371, déjà clos) est
également prêt.

## Point d'attention outillage

`npx vue-tsc --noEmit` et `.venv/bin/python manage.py check` (depuis la
racine du repo) fiables pour les deux tickets, chacun vérifié
indépendamment par l'agent `reviewer`. Toujours pas de script
`type-check`/`lint` dans `package.json`, toujours pas de
`.claude/launch.json` côté front — vérification par type-check + revue de
code uniquement pour #372 (pas de QA navigateur TV réelle en session
automatisée ; géométrie vérifiée par calcul analytique sur les propriétés
CSS réelles).

## Point d'attention protocole (reviewer)

Les trois agents `reviewer` invoqués cette session (une spec review dédiée +
deux reviews de ticket) ont strictement respecté leur mandat de lecture
seule — pattern désormais stable sur au moins 18 sessions consécutives
(#140-#164, sessions à vide comprises) depuis l'incident initial de la
session #139. Aucun `ScheduleWakeup` utilisé en attente des agents
asynchrones.

## Observation annexe (signalée depuis la session #144, toujours non actionnée)

Deux dossiers de sprint orphelins subsistent dans `backlog/sprints/` — hors
de `done/` et non référencés par `roadmap.md` : `04-admin-panel-map/` et
`10-contexte-url/`. À investiguer par l'utilisateur avant de les considérer
comme travail réellement en attente ou comme reliquats à archiver.
