# Session 2026-07-03 — N°61

**Sprint actif :** 18 — Tableau final conforme (4ᵉ session).
**Tickets traités :** 2 — #176, #230.

## Spec Review
| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/screens/admin-tableau-final.md` | ⚠️ Dérive mineure | 0 nouvelle — 3 dérives déjà connues confirmées (#176, #230, #231) |

Les 4 corrections précédentes du sprint (sélecteur d'épreuve, polling 4s sans
double-fetch, état INSCRIPTION, ConfirmModal de recréation) tiennent toutes
sans régression cumulée malgré 4 sessions successives de modifications sur
`AdminBracket.vue`.

## Tickets clôturés
| # | Titre | Verdict |
|---|-------|---------|
| [#176](https://github.com/sirius911/moutilloux/issues/176) | stage_label de _pack_match ne couvre pas P3 (petite finale) | ✅ Approuvé |
| [#230](https://github.com/sirius911/moutilloux/issues/230) | Bracket.p3 typé optionnel côté front alors que l'API le renvoie toujours | ✅ Approuvé |

### Détail #176
`live/api_views.py` : `Match.Stage.P3: "3e place"` ajouté au dict `labels` de
`stage_label()` dans `_pack_match` — texte exact conforme à
`specs/technical/cycle-de-vie-epreuve.md:283` et au label `Match.Stage.P3` de
`live/models.py`. Le reviewer a confirmé un doublon actif (pas du code mort)
dans `live/views.py::score_state` (même défaut, consommé par le scoreboard TV
via `stores/live.ts`) — ticketé séparément en #233 plutôt que corrigé ici,
hors périmètre strict du ticket. `manage.py check` OK.

### Détail #230
`frontend/app/src/types/index.ts:193` : `p3?: BracketSlot[]` → `p3:
BracketSlot[]` (retrait du `?`), conforme au contrat réel de
`_pack_event_bracket` qui renvoie toujours la clé `p3`. Aucun consommateur
cassé. `npx vue-tsc --noEmit` OK.

## Nouveaux tickets créés
| # | Titre | Sévérité |
|---|-------|----------|
| [#233](https://github.com/sirius911/moutilloux/issues/233) | live/views.py::score_state a le même défaut que #176 (stage_label P3 vide) | 🟡 mineure |

## Tickets à reprendre
| # | Titre | Raison |
|---|-------|--------|
| — | — | — |

## Fichiers partagés câblés
- Aucun. `live/urls.py`, `stores/event.ts`, `router/index.ts` non touchés.

## Problèmes d'orchestration
**Écart au protocole (§4, règle « séquentiel ») :** l'implémentation de #230
(agent vue-screen, fichier `frontend/app/src/types/index.ts`) a été lancée
avant que la review de #176 (agent django-api, fichier `live/api_views.py`)
ne soit terminée — les deux tickets ont donc tourné en partie en parallèle au
lieu d'être strictement séquentiels comme l'exige le protocole. Justification
a posteriori : les deux tickets touchent des fichiers strictement disjoints
(back Python vs type front), donc aucun risque réel de conflit ou de
contamination — confirmé par les deux agents et les deux reviewers, qui ont
chacun vérifié explicitement que le diff de l'autre ticket n'était pas mélangé
au leur. Aucune casse constatée, mais c'est un écart réel à la règle stricte
du protocole, à corriger dans les sessions suivantes (revenir à un traitement
strictement séquentiel, même quand les fichiers sont disjoints).

## Git
- Branche : `claude/sprint/18-tableau-final-conformite`
- Parent effectif : `claude/sprint/17-panneau-edition-match` (sprint 17 clos
  côté planification/milestone depuis la session #57, mais sa PR #223 n'est
  toujours pas mergée dans `main`). Déjà synchronisé cette session, aucun
  merge nécessaire.
- Push : ✅ succès

| Hash court | Message du commit |
|-----------|------------------|
| `7b0de64` | 18-176 ✅ stage_label : libellé « 3e place » pour P3 |
| `332cef7` | 18-230 ✅ Type Bracket.p3 : non-optionnel, conforme au contrat API |
