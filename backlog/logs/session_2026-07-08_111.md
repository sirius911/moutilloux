# Session 2026-07-08 — N°111

**Sprint actif :** 27 — Poules : suivi & suppression
**Tickets traités :** 2

## Spec Review

| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `admin-poules.md` | ❌ Dérive bloquante (attendu, sprint pas démarré) | 0 nouvelle (3 dérives confirmées dans le code, toutes déjà couvertes par #292-296) |

## Tickets clôturés

| # | Titre | Verdict |
|---|-------|---------|
| #292 | Back : service + endpoint de suppression de poule (POST /api/groups/<id>/delete/) | ✅ Approuvé |
| #294 | Back : api_event_groups — exposer les matchs de chaque poule (_pack_match) | ⚠️ Approuvé avec réserves (N+1 sur `group` détecté par la review, corrigé dans la session) |

## Nouveaux tickets créés

Aucun.

## Tickets à reprendre

Aucun.

## Fichiers partagés câblés

- `live/urls.py` : route `api/groups/<int:group_id>/delete/` → `api_views.api_group_delete` (ticket #292).

## Problèmes d'orchestration

Aucun écart au protocole. Point de qualité : la review de #294 a détecté un
N+1 réel introduit par l'implémentation (et déjà présent dans le plan
`backlog/plan/294-groups-matches.md` lui-même — `select_related` omettait
`"group"`, alors que `_pack_match` déréférence systématiquement
`match.group.name` pour tout match `stage=GROUP`). Corrigé directement par
l'orchestrateur (ajout de `"group"` au `select_related`,
`live/api_views.py:501`) avant clôture du ticket, plutôt que de laisser une
réserve trainer — `manage.py check` revérifié après correction.

## Git

- Branche : `claude/sprint/27-poules-suivi-suppression`
- Parent effectif : `claude/sprint/26-qualification-poule-terminee` (sprint 26 toujours non mergé dans `origin/main`)
- Push : voir plus bas

| Hash court | Message du commit |
|-----------|------------------|
| `8ca3362` | 27-292 ✅ Back : service + endpoint de suppression de poule |
| `3c53b4c` | 27-294 ⚠️ Back : api_event_groups expose les matchs de chaque poule |
