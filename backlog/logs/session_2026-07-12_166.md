# Session 2026-07-12 — N°166

**Sprint actif :** 43 — Correctifs retours du 11 juillet
**Tickets traités :** 2

## Spec Review

| Spec | Verdict | Nouvelles dérives |
|------|---------|------------------|
| `specs/screens/tv-live.md` | ✅ Conforme | 0 (2 dérives résiduelles = `#379`/`#375`, déjà ouvertes) |
| `specs/technical/tv-state.md` | ✅ Conforme | 0 |
| `specs/screens/arbitre-match.md` | ⚠️ Dérive mineure | 1 nouvelle (`#382`) + 1 note doc non bloquante |
| `specs/screens/admin-inscriptions.md` | ⚠️ Dérive mineure | 0 (dérive = `#378`, déjà ouverte, close en cours de session) + 1 note à trancher |
| `specs/technical/planning.md` | ✅ Conforme | 0 (dérive résiduelle = `#375`/`#380`, déjà ouvertes) |

Confiée à un agent `reviewer` dédié (lecture seule) en tout début de
session. Toutes les dérives résiduelles correspondent aux issues déjà
ouvertes (`#375`, `#378`, `#379`, `#380`), sauf une nouvelle **dérive
surprenante** sur `arbitre-match.md` : le flag `swap` (inversion des côtés)
n'est stocké qu'en session Django (`live/referee_views.py:57-58`) et jamais
exposé au front — `ArbitreMatch.vue` réinitialise son ref local `swapped` à
chaque montage, désynchronisant l'affichage de la logique serveur après un
reload en cours de match swappé. Nouvelle issue créée : `#382`.

Deux notes non ticketées (jugement produit, pas des bugs clairs) :
- `arbitre-match.md` nomme l'action `terminer`, le code utilise
  `finish_winner` — divergence de nom dans le texte de la spec, cosmétique,
  laissée en l'état (specs/ = lecture seule hors champ `fichiers:`, pas de
  correction de contenu par l'orchestrateur).
- `admin-inscriptions.md` : le bouton « Retirer » bifurque silencieusement
  vers une modale de forfait quand l'épreuve est `EN_COURS`, plutôt que de
  toujours tenter le retrait sec et laisser l'admin voir l'erreur serveur
  décrite littéralement par la spec — amélioration UX plausible mais
  divergente du texte ; à trancher par un humain, pas actionné automatiquement.

Fixé directement par l'orchestrateur (housekeeping, hors ticket) : ajout de
`frontend/app/src/views/tv/TvPalmares.vue` (livré au ticket `#374`, session
#165) à l'en-tête `fichiers:` de `tv-live.md`, qui ne le listait pas encore.

## Tickets clôturés

| # | Titre | Verdict |
|---|-------|---------|
| #375 | Back : ETA à la lecture — service commun + scheduledTime recalé dans les packers | ⚠️ Approuvé avec réserves |
| #378 | Front : CreateTeamModal — exclure les joueurs déjà engagés dans une équipe de l'épreuve | ✅ Approuvé |

### #375 (majeure)

`live/admin_views.py` (nouvelle fonction de service `compute_day_eta_map` +
helpers, purement additive) + `live/api_views.py` (`_pack_match` gagne un
paramètre optionnel `eta_display=None` rétrocompatible, branché sur les 4
surfaces citées par le ticket : `_pack_calendar_play_days` → calendrier
admin + programme arbitre, `api_tv_state`/next, `api_arbitre_matches`/next,
`_pack_tv_programme`/upcoming). Simplification retenue (documentée dans le
plan `backlog/plan/375-eta-service-commun.md`) : les matchs LIVE/FINISHED
avec `started_at`/`finished_at` posé affichent directement l'heure réelle
sans rejouer le curseur — seul le curseur monotone (recalé par « maintenant »)
s'applique aux SCHEDULED. Confié à un agent `django-api`. Reviewer : logique
de curseur tracée à la main et comparée ligne à ligne au moteur front
`AdminMatches.vue::etaEngine` (alignement exact, y compris le tri mixte
volontairement identique order_index global/local) ; 15 appels existants de
`_pack_match(m)` sans le nouveau paramètre confirmés non cassés ; pas de
cycle d'import ; `manage.py check` 0 erreur (vérifié indépendamment) ; smoke
test réel sur la base de dev sans exception. Verdict : ⚠️ Approuvé avec
réserves — la seule réserve concernait des fichiers hors périmètre
(`log.md`/`tv-live.md`, résidus de la spec review de cette même session)
détectés dans l'arbre de travail au moment de la review ; exclus du commit
de ce ticket, committés séparément (voir plus bas). Une suggestion non
bloquante sur la mémoïsation du calcul par requête, déjà anticipée par le
ticket lui-même.

### #378 (mineure)

`frontend/app/src/components/modals/CreateTeamModal.vue` — nouveau
`computed` `playersInTeams` (dérivé de `eventStore.players`, sans filtrer
sur `withdrawn` pour rester cohérent avec le garde-fou serveur `#377` qui
n'en tient pas compte non plus), filtre étendu dans `filteredForSlot()`.
Confié à un agent `vue-screen` (pas de portage d'écran, modification ciblée
d'un composant existant, plan `backlog/plan/378-createteammodal-exclure-joueurs-en-equipe.md`).
Reviewer : diff strictement conforme au plan ; edge case joueur Simple
confirmé non affecté (`entry.player`/`entry.team` mutuellement exclusifs) ;
`AdminInscriptions.vue` confirmé inutile à modifier (`eventStore.createTeam()`
recharge déjà `players` après création) ; message d'erreur serveur (filet)
conservé ; `npx vue-tsc --noEmit` 0 erreur (vérifié indépendamment). Verdict :
✅ Approuvé, aucune réserve.

## Nouveaux tickets créés

| # | Titre | Sévérité |
|---|-------|----------|
| #382 | Arbitre : le swap (inversion des côtés) n'est pas resynchronisé après un rechargement de page | 🟠 Majeure |

## Tickets à reprendre

Aucun.

## Fichiers partagés câblés

Aucun cette session — les deux tickets touchaient exclusivement leurs
propres fichiers back/front (pas de `live/urls.py`, pas de store/composable
Pinia, pas de route).

## Problèmes d'orchestration

Aucun écart de protocole. Point relevé par le reviewer de #375 (sans
impact) : au moment de sa review, l'arbre de travail contenait encore les
modifications de housekeeping de la spec review (`log.md`, `tv-live.md`)
faites plus tôt dans cette même session — correctement identifiées comme
hors périmètre du ticket #375 et exclues de son commit par un `git add`
ciblé sur les deux seuls fichiers du plan.

## Git

- Branche : `claude/sprint/43-retours-11-juillet`
- Parent effectif : `main` (déjà à jour au démarrage, `git merge-base
  --is-ancestor origin/main HEAD` positif d'emblée, aucun merge ni commit de
  rattrapage nécessaire)
- Push : en attente (fin d'étape 4)

| Hash court | Message du commit |
|-----------|------------------|
| `3d06ccc` | 43-375 ⚠️ Back : ETA à la lecture — service commun + scheduledTime recalé |
| `d68d3af` | 43-378 ✅ CreateTeamModal : exclure les joueurs déjà en équipe |
